#!/bin/bash
set -euo pipefail

end=$((SECONDS + 60))
while [ "$SECONDS" -lt "$end" ]; do
    if busctl --user status org.kde.KWin >/dev/null 2>&1; then
        exit 0
    fi
    sleep 1
done

echo "timed out waiting for the kde session" >&2
exit 1
